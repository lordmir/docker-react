language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -f Dockerfile.dev -t lordmir/docker-react .

script:
  - docker run -e CI=true lordmir/docker-react npm run test